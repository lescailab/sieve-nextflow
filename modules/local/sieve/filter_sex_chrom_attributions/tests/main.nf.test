nextflow_process {

    name "Test Process SIEVE_FILTER_SEX_CHROM_ATTRIBUTIONS"
    script "../main.nf"
    process "SIEVE_FILTER_SEX_CHROM_ATTRIBUTIONS"
    config "./nextflow.config"

    tag "modules"
    tag "local"
    tag "filter_sex_chrom_attributions"

    test("stub run") {

        options "-stub"

        when {
            process {
                """
input[0] = [
    [ id: 'cohort' ],
    file("${projectDir}/assets/testdata/parsed_clinvar.tsv", checkIfExists: true),
    file("${projectDir}/assets/testdata/parsed_gwas.tsv", checkIfExists: true)
]
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert process.out.filtered_rankings != null },
                { assert process.out.versions != null }
            )
        }
    }
}
