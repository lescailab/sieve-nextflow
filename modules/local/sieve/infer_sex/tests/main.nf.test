nextflow_process {

    name "Test Process SIEVE_INFER_SEX"
    script "../main.nf"
    process "SIEVE_INFER_SEX"
    config "./nextflow.config"

    tag "modules"
    tag "local"
    tag "infer_sex"

    test("stub run") {

        options "-stub"

        when {
            process {
                """
input[0] = [
    [ id: 'cohort' ],
    file("${projectDir}/assets/testdata/small_test_data.vcf.gz", checkIfExists: true),
    file("${projectDir}/assets/testdata/small_test_data.vcf.gz.csi", checkIfExists: true)
]
input[1] = 'GRCh38'
                """
            }
        }

        then {
            assertAll(
                { assert process.success },
                { assert process.out.sex_map != null },
                { assert process.out.versions != null }
            )
        }
    }
}
