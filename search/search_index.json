{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"SIEVE Nextflow","text":"<p>Development status</p> <p>This pipeline is under active development on the <code>dev</code> branch and has no tagged release. Parameters, output paths and default values can change between commits. Pin a commit when you need a reproducible run, as described in Reproducibility.</p> <p>This site documents the Nextflow pipeline that orchestrates SIEVE. The scientific method, the model, the encoding, the explainability procedures and the command-line tools live in <code>lescailab/sieve-project</code>, documented at SIEVE Documentation. This repository wraps those tools in a reproducible workflow: it manages inputs, provisions containers, schedules the steps in dependency order, handles resource allocation and retries, and publishes results in a fixed layout. Read the framework documentation to understand what each step computes. Read this site to understand how to run the steps together.</p> <p>The repository also keeps quick-access documentation in the root README, usage guide, and output guide.</p>"},{"location":"#documentation-layers","title":"Documentation layers","text":"Layer Audience Purpose <code>README.md</code> First-time users Run a smoke test, identify minimal inputs, and find the main docs. <code>docs/usage.md</code> Routine users Check verified parameters, profiles, and common execution patterns. <code>docs/output.md</code> Routine users Locate published result files. <code>guidelines/</code> Users and maintainers Understand concepts, configuration, workflow steps, failure modes, and extension points."},{"location":"#what-the-pipeline-does","title":"What the pipeline does","text":"<p>The pipeline takes a bgzipped, indexed, multi-sample VCF plus phenotype data, prepares SIEVE-ready tensors, trains models, selects checkpoints, produces explainability rankings, compares real and null attribution patterns, and can run ablation, candidate-interaction testing with power analysis, validation, and plot-collection branches.</p> <p>The default execution requests all defined steps:</p> <pre><code>sex -&gt; preprocess -&gt; grid -&gt; cv -&gt; explain\n                       |       |\n                       |       +-&gt; null -&gt; attribution comparison\n                       |\n                       +-&gt; ablation -&gt; per-level attribution comparison\nexplain + preprocess -&gt; epistasis\nexplain + references  -&gt; validation\nall plot sources      -&gt; plots\n</code></pre>"},{"location":"#verified-facts","title":"Verified facts","text":"<ul> <li>Entry point: <code>main.nf</code></li> <li>Named workflow: <code>NFCORE_SIEVE</code>, which calls <code>workflows/sieve.nf</code></li> <li>Primary workflow: <code>SIEVE</code></li> <li>Minimum Nextflow version in the manifest: <code>&gt;=25.04.0</code></li> <li>Parameter schema: <code>nextflow_schema.json</code></li> <li>Runtime plugin: <code>nf-schema@2.5.1</code></li> <li>Main config: <code>nextflow.config</code></li> <li>Module-specific config: <code>conf/modules.config</code></li> <li>Resource defaults: <code>conf/base.config</code></li> <li>Test profile: <code>conf/test.config</code></li> <li>Verified smoke command:</li> </ul> <pre><code>nextflow run . \\\n  -profile test \\\n  -stub-run \\\n  --outdir results_stub\n</code></pre>"},{"location":"#how-to-use-this-site","title":"How to use this site","text":"<p>Start with Getting started if you need to run the pipeline. Use Pipeline overview and Workflow steps to understand execution logic. Use Configuration, Troubleshooting, and Reproducibility when moving from test data to a controlled analysis run.</p>"},{"location":"#standards-followed","title":"Standards followed","text":"<p>This pipeline follows nf-core structural conventions without being an nf-core pipeline. Its documentation is organised around the nf-core expectation that bundled pipeline documentation includes a README, usage guide, and output guide, and around the Nextflow training style of moving from runnable examples to concepts and then advanced configuration.</p> <p>Reference material:</p> <ul> <li>nf-core pipeline specifications</li> <li>nf-core bundled documentation requirement</li> <li>Nextflow training: Hello Nextflow</li> <li>Material for MkDocs</li> </ul>"},{"location":"configuration/","title":"Configuration","text":"<p>SIEVE follows the standard Nextflow configuration model: base defaults are declared in <code>nextflow.config</code>, included config files add resource and module behavior, profiles override runtime settings, and command-line parameters have the final say.</p>"},{"location":"configuration/#configuration-files","title":"Configuration files","text":"File Role <code>nextflow.config</code> Default parameters, profiles, manifest, reports, environment variables, plugin declaration, and included configs. <code>conf/base.config</code> Process resource defaults and labels. <code>conf/modules.config</code> Module-specific process options. Currently configures compare-attribution genome-build arguments. <code>conf/test.config</code> Bundled test data and smaller grid for test runs. <code>conf/test_full.config</code> Full-test profile wrapper; currently includes <code>test.config</code>. <code>conf/google_batch_a100_no_fusion.config</code> Google Batch executor with A100 GPU and standard GCS staging (no Fusion). <code>nextflow_schema.json</code> Parameter schema for validation and help text."},{"location":"configuration/#core-defaults","title":"Core defaults","text":"<p>Important defaults from <code>nextflow.config</code>:</p> Parameter Default Notes <code>--outdir</code> <code>results</code> Root publish directory. <code>--cohort_id</code> <code>cohort</code> Published under <code>&lt;outdir&gt;/&lt;cohort_id&gt;/</code>. <code>--genome_build</code> <code>GRCh38</code> Allowed values: <code>GRCh37</code>, <code>GRCh38</code>. <code>--infer_sex</code> <code>true</code> Used when sex information is needed and no <code>--sex_map</code> is supplied. <code>--default_train_level</code> <code>L3</code> Main training level. <code>--cv_folds</code> <code>5</code> CV folds for <code>SIEVE_TRAIN_CV</code>. <code>--train_device</code> <code>cuda</code> Passed to SIEVE training. <code>--publish_dir_mode</code> <code>copy</code> Sets <code>workflow.output.mode</code>."},{"location":"configuration/#resource-labels","title":"Resource labels","text":"<p><code>conf/base.config</code> defines labels used by modules:</p> Label Default resources <code>process_single</code> 1 CPU, 6 GB memory, 12 h <code>process_low</code> 2 CPUs, 24 GB memory, 24 h <code>process_medium</code> 6 CPUs, 72 GB memory, 48 h <code>process_high</code> 12 CPUs, 128 GB memory, 72 h <code>process_long</code> 120 h <code>process_high_memory</code> 256 GB memory <code>process_gpu</code> 72 h per attempt, optional accelerator when profile contains <code>gpu</code> <p>The defaults scale with task attempts where configured.</p>"},{"location":"configuration/#profiles","title":"Profiles","text":"<p>Profiles are selected with <code>-profile</code>, not with <code>--profile</code>:</p> <pre><code>nextflow run . -profile test -stub-run --outdir results_stub\n</code></pre> <p>Combine profiles with commas:</p> <pre><code>nextflow run . -profile docker,gpu -params-file params.yml\n</code></pre>"},{"location":"configuration/#reports","title":"Reports","text":"<p>Nextflow reports are enabled by default:</p> <pre><code>pipeline_info/execution_timeline_&lt;timestamp&gt;.html\npipeline_info/execution_report_&lt;timestamp&gt;.html\npipeline_info/execution_trace_&lt;timestamp&gt;.txt\npipeline_info/pipeline_dag_&lt;timestamp&gt;.html\npipeline_info/params_&lt;timestamp&gt;.json\npipeline_info/nf_core_sieve_software_versions.yml\n</code></pre> <p>The suffix is controlled by <code>--trace_report_suffix</code>.</p>"},{"location":"configuration/#custom-configs","title":"Custom configs","text":"<p>The root config includes nf-core custom config support through <code>params.custom_config_base</code>. For offline or tightly controlled runs, check whether your environment should set <code>NXF_OFFLINE=true</code> or pass a local custom config.</p>"},{"location":"configuration/#command-line-precedence","title":"Command-line precedence","text":"<p>Use command-line values for short-lived overrides:</p> <pre><code>nextflow run . \\\n  -profile docker \\\n  --train_device cpu \\\n  --outdir results_cpu\n</code></pre> <p>Use <code>-params-file</code> for repeatable runs:</p> <pre><code>nextflow run . \\\n  -profile docker,gpu \\\n  -params-file params.yml\n</code></pre>"},{"location":"extending-the-pipeline/","title":"Extending the Pipeline","text":"<p>This page is for maintainers adding or changing workflow behavior.</p>"},{"location":"extending-the-pipeline/#follow-the-existing-structure","title":"Follow the existing structure","text":"<p>Current structure:</p> <pre><code>main.nf\nworkflows/sieve.nf\nsubworkflows/local/\nmodules/local/sieve/\nconf/\ntests/\ndocs/\nguidelines/\n</code></pre> <p>Add workflow composition in <code>subworkflows/local/</code> and single-command wrappers in <code>modules/local/sieve/</code>.</p>"},{"location":"extending-the-pipeline/#keep-modules-focused","title":"Keep modules focused","text":"<p>Existing local modules wrap one SIEVE command or one small Python helper. Match that pattern:</p> <ul> <li>One process per tool command or helper.</li> <li>Inputs and outputs declared explicitly.</li> <li><code>conda</code> and <code>container</code> directives present.</li> <li><code>stub:</code> block produces representative output files.</li> <li>Versions emitted through <code>topic: versions</code> or <code>versions.yml</code>.</li> </ul>"},{"location":"extending-the-pipeline/#update-configuration","title":"Update configuration","text":"<p>When adding a parameter:</p> <ol> <li>Add the default to <code>nextflow.config</code>.</li> <li>Add validation and help text to <code>nextflow_schema.json</code>.</li> <li>Add validation logic to <code>lib/sieve_helpers.nf</code> only if Nextflow must catch the error before a process runs.</li> <li>Document the parameter in <code>docs/usage.md</code> only if it is user-facing.</li> <li>Add deeper explanation in <code>guidelines/</code> when needed.</li> </ol>"},{"location":"extending-the-pipeline/#update-publishing","title":"Update publishing","text":"<p>Published results are controlled in <code>main.nf</code>:</p> <pre><code>publish:\n...\n\noutput {\n    ...\n}\n</code></pre> <p>If a new output is user-facing, add it to:</p> <ul> <li><code>docs/output.md</code> for concise location and meaning</li> <li><code>guidelines/outputs-detailed.md</code> for detailed context</li> </ul>"},{"location":"extending-the-pipeline/#test-changes","title":"Test changes","text":"<p>Use repository tooling through the expected Conda environment:</p> <pre><code>conda run -n nf-core_3.5.2 nextflow run . -profile test -stub-run --outdir results_stub\nconda run -n nf-core_3.5.2 nf-test test\nconda run -n nf-core_3.5.2 nf-core pipelines lint\n</code></pre> <p>Broaden tests when changing shared channels, <code>--execute_step</code> behavior, or output publishing.</p>"},{"location":"extending-the-pipeline/#documentation-rules","title":"Documentation rules","text":"<p>Keep the two-layer strategy:</p> <ul> <li>README, <code>docs/usage.md</code>, and <code>docs/output.md</code> stay concise and immediately actionable.</li> <li><code>guidelines/</code> carries tutorials, conceptual explanation, assumptions, and troubleshooting.</li> </ul> <p>Do not duplicate long explanations between layers. Link from concise docs to the relevant guidelines page.</p>"},{"location":"extending-the-pipeline/#privacy-rule","title":"Privacy rule","text":"<p>Never commit real dataset paths, cohort names, server names, or phenotype labels in examples. Use placeholders:</p> <pre><code>/path/to/data.vcf.gz\ncohort_a\nphenotype_x\nyour_server\n</code></pre> <p>Before staging documentation or tests, scan for real paths and identifiers.</p>"},{"location":"getting-started/","title":"Getting Started","text":"<p>This page takes the shortest path from a clean checkout to a verified pipeline launch.</p>"},{"location":"getting-started/#1-check-the-software-entry-point","title":"1. Check the software entry point","text":"<p>The pipeline entry point is <code>main.nf</code>. From the repository root, use:</p> <pre><code>nextflow run .\n</code></pre> <p>From outside the checkout, use the repository name:</p> <pre><code>nextflow run lescailab/sieve-nextflow\n</code></pre>"},{"location":"getting-started/#2-run-a-smoke-test","title":"2. Run a smoke test","text":"<p>Use bundled test data and stubbed commands:</p> <pre><code>nextflow run . \\\n  -profile test \\\n  -stub-run \\\n  --outdir results_stub\n</code></pre> <p>Why this is useful:</p> <ul> <li><code>-profile test</code> supplies small VCF, phenotype, sex-map, and validation-resource test files from <code>conf/test.config</code>.</li> <li><code>-stub-run</code> exercises the workflow graph without running the real SIEVE command-line tools.</li> <li>The command verifies that the Nextflow entry point, configuration, process graph, and publish layout are coherent.</li> </ul>"},{"location":"getting-started/#3-inspect-the-output","title":"3. Inspect the output","text":"<p>The smoke test publishes:</p> <pre><code>results_stub/\n\u251c\u2500\u2500 cohort/\n\u2502   \u251c\u2500\u2500 data/\n\u2502   \u251c\u2500\u2500 real_experiments/\n\u2502   \u251c\u2500\u2500 null_baselines/\n\u2502   \u251c\u2500\u2500 attribution_comparison/\n\u2502   \u251c\u2500\u2500 ablation/\n\u2502   \u251c\u2500\u2500 epistasis/\n\u2502   \u2514\u2500\u2500 validation/\n\u2514\u2500\u2500 pipeline_info/\n</code></pre> <p>The default cohort directory is <code>cohort</code>. Override it with <code>--cohort_id</code>, using a generic identifier such as <code>cohort_a</code>.</p>"},{"location":"getting-started/#4-prepare-real-inputs","title":"4. Prepare real inputs","text":"<p>For raw-input mode, prepare:</p> <ul> <li>A bgzipped multi-sample VCF: <code>/path/to/data.vcf.gz</code></li> <li>A VCF index beside it: <code>/path/to/data.vcf.gz.tbi</code> or <code>/path/to/data.vcf.gz.csi</code></li> <li>A phenotype TSV accepted by <code>sieve-preprocess</code></li> <li>A genome build: <code>GRCh37</code> or <code>GRCh38</code></li> <li>Either a sex map or enough VCF information for sex inference</li> </ul> <p>The Nextflow layer validates the VCF extension, the index, file existence, and the genome-build value. It does not validate phenotype columns; malformed phenotype files fail inside <code>sieve-preprocess</code>.</p>"},{"location":"getting-started/#5-run-a-real-analysis","title":"5. Run a real analysis","text":"<p>The default training device is CUDA. For a GPU run with Docker:</p> <pre><code>nextflow run lescailab/sieve-nextflow \\\n  -profile docker,gpu \\\n  --vcf /path/to/data.vcf.gz \\\n  --phenotypes /path/to/phenotypes.tsv \\\n  --genome_build GRCh38 \\\n  --outdir results\n</code></pre> <p>For a CPU trial, override the SIEVE training device explicitly:</p> <pre><code>nextflow run lescailab/sieve-nextflow \\\n  -profile docker \\\n  --vcf /path/to/data.vcf.gz \\\n  --phenotypes /path/to/phenotypes.tsv \\\n  --genome_build GRCh38 \\\n  --train_device cpu \\\n  --outdir results\n</code></pre> <p>CPU runs are expected to be slower for training-heavy steps.</p>"},{"location":"getting-started/#6-read-the-concise-docs","title":"6. Read the concise docs","text":"<p>Use the root docs for quick reference:</p> <ul> <li>README</li> <li>Usage</li> <li>Output</li> </ul>"},{"location":"inputs/","title":"Inputs","text":"<p>This page distinguishes required raw inputs from optional shortcut artifacts.</p>"},{"location":"inputs/#raw-inputs","title":"Raw inputs","text":"Input Parameter Verified checks in the Nextflow layer Multi-sample VCF <code>--vcf</code> File exists, ends with <code>.vcf.gz</code>, and has <code>.tbi</code> or <code>.csi</code> sibling index. Phenotype TSV <code>--phenotypes</code> File exists. Detailed format validation is delegated to <code>sieve-preprocess</code>. Genome build <code>--genome_build</code> Must be <code>GRCh37</code> or <code>GRCh38</code>. Default: <code>GRCh38</code>. Sex metadata <code>--sex_map</code> or <code>--infer_sex true</code> If selected steps need sex information and no sex map is supplied, sex inference must be enabled. <p>Phenotype TSV format (confirmed from bundled test data and SIEVE CLI contract):</p> <pre><code>sample1 2\nsample2 1\n</code></pre> <p>Two tab-separated columns, no header row. Column 1 is the sample identifier; column 2 is an integer class label (<code>1</code> or <code>2</code>). The Nextflow layer checks only that the file exists; detailed parsing and validation are delegated to the <code>sieve-preprocess</code> CLI.</p>"},{"location":"inputs/#vcf-index-naming","title":"VCF index naming","text":"<p>For:</p> <pre><code>/path/to/data.vcf.gz\n</code></pre> <p>the validator accepts either:</p> <pre><code>/path/to/data.vcf.gz.tbi\n/path/to/data.vcf.gz.csi\n</code></pre>"},{"location":"inputs/#sex-map-inputs","title":"Sex-map inputs","text":"<p>The effective sex map is published as:</p> <pre><code>&lt;outdir&gt;/&lt;cohort_id&gt;/data/sample_sex.tsv\n</code></pre> <p>If <code>--sex_map</code> is supplied, <code>SIEVE_EMIT_SEX_MAP</code> copies it into that standardized filename. If <code>--sex_map</code> is absent and sex information is needed, <code>SIEVE_INFER_SEX</code> calls:</p> <pre><code>sieve-infer-sex --vcf &lt;vcf&gt; --output-dir infer_sex_diagnostics --genome-build &lt;build&gt;\n</code></pre> <p><code>--known_sex</code> is only passed to sex inference for concordance checking. It does not replace <code>--sex_map</code>.</p>"},{"location":"inputs/#shortcut-artifacts","title":"Shortcut artifacts","text":"Artifact Parameter Format check Used to skip Preprocessed tensor <code>--preprocessed_data</code> Existing <code>.pt</code> VCF preprocessing Best parameter file <code>--best_params</code> Existing <code>.yaml</code> or <code>.yml</code> Grid search Best checkpoint <code>--best_checkpoint</code> Existing <code>.pt</code> CV checkpoint training and selection Checkpoint config <code>--checkpoint_config</code> Existing <code>.yaml</code> or <code>.yml</code> Required with <code>--best_checkpoint</code> <p>The validator rejects <code>--best_checkpoint</code> without <code>--checkpoint_config</code>, and rejects <code>--checkpoint_config</code> without <code>--best_checkpoint</code>.</p>"},{"location":"inputs/#optional-validation-resources","title":"Optional validation resources","text":"<p>These are used only when <code>validation</code> is selected:</p> <ul> <li><code>--clinvar_tsv</code></li> <li><code>--gwas_tsv</code></li> <li><code>--go_mapping_json</code></li> </ul> <p>If none are supplied, the workflow runs <code>SIEVE_DOWNLOAD_REFERENCES</code> to fetch all three. If any subset is supplied, the missing ones are represented internally as absent (not downloaded). The workflow does not perform partial downloads when at least one reference is already provided.</p>"},{"location":"inputs/#input-privacy","title":"Input privacy","text":"<p>Keep real dataset paths, cohort identifiers, and phenotype labels out of committed files. Use placeholders in shared documentation and local parameter files for execution.</p>"},{"location":"installation/","title":"Installation","text":"<p>The pipeline can run with containers, Conda/Mamba environments, or software already available on the execution host. Containers are the most reproducible route for normal users.</p>"},{"location":"installation/#required-runtime","title":"Required runtime","text":"<p>The pipeline manifest declares:</p> <pre><code>nextflowVersion = !&gt;=25.04.0\n</code></pre> <p>Install a compatible Nextflow release before running the pipeline. The verified local smoke test used Nextflow 25.10.3.</p>"},{"location":"installation/#nextflow-plugin","title":"Nextflow plugin","text":"<p><code>nextflow.config</code> enables:</p> <pre><code>plugins {\n    id 'nf-schema@2.5.1'\n}\n</code></pre> <p>The plugin validates parameters against <code>nextflow_schema.json</code> and renders the help text.</p>"},{"location":"installation/#software-dependencies","title":"Software dependencies","text":"<p>Local SIEVE modules define both:</p> <ul> <li><code>conda \"${moduleDir}/environment.yml\"</code></li> <li>a container image pinned as <code>ghcr.io/lescailab/sieve-container:97c6b69bbe1c73b2</code></li> </ul> <p>Use one of the software profiles:</p> <pre><code>-profile docker\n-profile singularity\n-profile apptainer\n-profile conda\n-profile mamba\n</code></pre> <p>Training-heavy modules are labelled <code>process_gpu</code>. The <code>gpu</code> profile adds GPU runtime options and sets accelerators for GPU-aware execution.</p>"},{"location":"installation/#gpu-expectation","title":"GPU expectation","text":"<p>The pipeline default is:</p> <pre><code>--train_device cuda\n</code></pre> <p>For GPU container runs:</p> <pre><code>nextflow run lescailab/sieve-nextflow \\\n  -profile docker,gpu \\\n  --vcf /path/to/data.vcf.gz \\\n  --phenotypes /path/to/phenotypes.tsv \\\n  --outdir results\n</code></pre> <p>For CPU trials:</p> <pre><code>nextflow run lescailab/sieve-nextflow \\\n  -profile docker \\\n  --vcf /path/to/data.vcf.gz \\\n  --phenotypes /path/to/phenotypes.tsv \\\n  --train_device cpu \\\n  --outdir results\n</code></pre>"},{"location":"installation/#repository-maintenance-environment","title":"Repository maintenance environment","text":"<p>In this repository, Nextflow-related tooling is expected to run through the Conda environment named <code>nf-core_3.5.2</code>:</p> <pre><code>conda run -n nf-core_3.5.2 nextflow run . -profile test -stub-run --outdir results_stub\nconda run -n nf-core_3.5.2 nf-test test\nconda run -n nf-core_3.5.2 nf-core pipelines lint\n</code></pre> <p>This is a contributor/development convention, not a requirement for external users who already have a compatible Nextflow installation.</p>"},{"location":"installation/#building-these-guidelines","title":"Building these guidelines","text":"<p>The guidelines use Material for MkDocs. Install it in a documentation environment:</p> <pre><code>pip install mkdocs-material\n</code></pre> <p>Preview the site from the repository root (<code>mkdocs.yml</code> lives at the repo root and is configured with <code>docs_dir: guidelines/</code>):</p> <pre><code>mkdocs serve\n</code></pre> <p>The local <code>nf-core_3.5.2</code> environment used for pipeline tooling does not currently provide <code>mkdocs</code>.</p>"},{"location":"outputs-detailed/","title":"Detailed Outputs","text":"<p>All paths are relative to <code>--outdir</code>. The cohort directory is <code>--cohort_id</code>, defaulting to <code>cohort</code>.</p> <p>Scientific interpretation</p> <p>This page identifies published files and their pipeline locations. Use the framework's Interpreting Results guide for scientific interpretation.</p>"},{"location":"outputs-detailed/#high-level-tree","title":"High-level tree","text":"<pre><code>&lt;outdir&gt;/\n\u251c\u2500\u2500 &lt;cohort_id&gt;/\n\u2502   \u251c\u2500\u2500 data/\n\u2502   \u251c\u2500\u2500 real_experiments/\n\u2502   \u251c\u2500\u2500 null_baselines/\n\u2502   \u251c\u2500\u2500 attribution_comparison/\n\u2502   \u251c\u2500\u2500 ablation/\n\u2502   \u251c\u2500\u2500 epistasis/\n\u2502   \u2514\u2500\u2500 validation/\n\u2514\u2500\u2500 pipeline_info/\n</code></pre>"},{"location":"outputs-detailed/#data","title":"Data","text":"<pre><code>&lt;cohort_id&gt;/data/sample_sex.tsv\n&lt;cohort_id&gt;/data/preprocessed.pt\n</code></pre> <p><code>sample_sex.tsv</code> is the effective sex map. It may be inferred or copied from <code>--sex_map</code>.</p> <p><code>preprocessed.pt</code> is the SIEVE-ready tensor dataset. It may be generated from raw inputs or copied from <code>--preprocessed_data</code>.</p>"},{"location":"outputs-detailed/#real-experiments","title":"Real experiments","text":"<p>Primary L3 outputs:</p> <pre><code>&lt;cohort_id&gt;/real_experiments/L3/cross_fold/cv_output/fold_*/\n&lt;cohort_id&gt;/real_experiments/L3/training/best_checkpoint.pt\n&lt;cohort_id&gt;/real_experiments/L3/training/best_fold_config.yaml\n&lt;cohort_id&gt;/real_experiments/L3/training/cv_folds_summary.tsv\n&lt;cohort_id&gt;/real_experiments/L3/attributions/explain_output/\n</code></pre> <p>Ablation real-training outputs are also published under:</p> <pre><code>&lt;cohort_id&gt;/real_experiments/L0/\n&lt;cohort_id&gt;/real_experiments/L1/\n&lt;cohort_id&gt;/real_experiments/L2/\n&lt;cohort_id&gt;/real_experiments/L3/\n</code></pre> <p>Inside attribution directories, key files include:</p> <pre><code>sieve_variant_rankings.csv\nsieve_gene_rankings.csv\nsieve_interactions.csv\nattributions.npz\n</code></pre>"},{"location":"outputs-detailed/#null-baselines","title":"Null baselines","text":"<pre><code>&lt;cohort_id&gt;/null_baselines/L*/training/\n&lt;cohort_id&gt;/null_baselines/L*/attributions/explain_output/\n</code></pre> <p>For the primary L3 null branch, training can include:</p> <pre><code>preprocessed_NULL.pt\nbest_model.pt\nconfig.yaml\nresults.yaml\ntraining_history.yaml\n</code></pre> <p>Per-level ablation null branches publish selection-payload directories containing:</p> <pre><code>best_model.pt\nconfig.yaml\nresults.yaml\n</code></pre>"},{"location":"outputs-detailed/#attribution-comparison","title":"Attribution comparison","text":"<pre><code>&lt;cohort_id&gt;/attribution_comparison/L*/\n\u251c\u2500\u2500 comparison_summary.yaml\n\u251c\u2500\u2500 calibrated_rankings.csv\n\u251c\u2500\u2500 comparison_output/\n\u2514\u2500\u2500 corrected/\n</code></pre> <p>Important files:</p> File Meaning <code>comparison_summary.yaml</code> Summary emitted by real-vs-null attribution comparison. <code>comparison_output/variant_rankings_with_significance.csv</code> Variant ranking table with significance fields when produced. <code>comparison_output/significant_variants.csv</code> Significant variant subset when produced. <code>calibrated_rankings.csv</code> Bootstrap-calibrated ranking table. <code>corrected/corrected_variant_rankings.csv</code> Variant rankings after chrX-bias correction. <code>corrected/corrected_gene_rankings.csv</code> Gene-level corrected rankings. <code>corrected/correction_report.yaml</code> Correction summary."},{"location":"outputs-detailed/#ablation","title":"Ablation","text":"<pre><code>&lt;cohort_id&gt;/ablation/comparison_levels/\n&lt;cohort_id&gt;/ablation/gene_significance_rankings_delta/L*/\n&lt;cohort_id&gt;/ablation/gene_significance_rankings_zattr/L*/\n&lt;cohort_id&gt;/ablation/variants_significance_rankings/\n</code></pre> <p>Key files:</p> <pre><code>ablation_summary.tsv\nablation_summary.yaml\nablation_jaccard_matrix.tsv\nlevel_specific_variants.tsv\nablation_comparison_plot.png\ngene_list_by_delta_rank.tsv\ngene_list_by_z_attribution.tsv\nvariant_significance_rankings.csv\n</code></pre> <p>The current output target for <code>variant_significance_rankings.csv</code> is shared across levels, so repeated L0-L3 emissions can publish to the same directory.</p>"},{"location":"outputs-detailed/#epistasis","title":"Epistasis","text":"<pre><code>&lt;cohort_id&gt;/epistasis/L3/epistasis_audit/\n&lt;cohort_id&gt;/epistasis/L3/gene_interactions/\n</code></pre> <p>Key files:</p> <pre><code>epistasis_validation.csv\ngene_pair_interactions.csv\ngene_interaction_network_edges.csv\ngene_interaction_network_nodes.csv\ngene_interaction_summary.yaml\n</code></pre> <p><code>epistasis_validation.csv</code> is emitted only when the real explainability interaction file contains non-empty interaction rows.</p>"},{"location":"outputs-detailed/#validation-and-plots","title":"Validation and plots","text":"<pre><code>&lt;cohort_id&gt;/validation/validation_report.yaml\n&lt;cohort_id&gt;/validation/validation_output/\n&lt;cohort_id&gt;/validation/plots/\n&lt;cohort_id&gt;/validation/plots_manifest.tsv\n</code></pre> <p>Validation can use supplied reference resources or downloaded references. Plot collection depends on plot-source directories produced by selected upstream steps.</p>"},{"location":"outputs-detailed/#pipeline-info","title":"Pipeline info","text":"<pre><code>pipeline_info/execution_timeline_&lt;timestamp&gt;.html\npipeline_info/execution_report_&lt;timestamp&gt;.html\npipeline_info/execution_trace_&lt;timestamp&gt;.txt\npipeline_info/pipeline_dag_&lt;timestamp&gt;.html\npipeline_info/params_&lt;timestamp&gt;.json\npipeline_info/nf_core_sieve_software_versions.yml\n</code></pre> <p>Use these files to audit command parameters, runtime behavior, task failures, and software versions.</p>"},{"location":"pipeline-overview/","title":"Pipeline Overview","text":"<p>This page describes the pipeline structure as implemented in the codebase.</p>"},{"location":"pipeline-overview/#entry-points","title":"Entry points","text":"<p><code>main.nf</code> imports three workflows:</p> <ul> <li><code>SIEVE</code> from <code>workflows/sieve.nf</code></li> <li><code>PIPELINE_INITIALISATION</code> from <code>subworkflows/local/utils_nfcore_sieve_pipeline</code></li> <li><code>PIPELINE_COMPLETION</code> from <code>subworkflows/local/utils_nfcore_sieve_pipeline</code></li> </ul> <p>The default <code>workflow {}</code> block performs this sequence:</p> <pre><code>PIPELINE_INITIALISATION(...)\nNFCORE_SIEVE()\nPIPELINE_COMPLETION(...)\n</code></pre> <p><code>NFCORE_SIEVE</code> is a named wrapper around <code>SIEVE()</code>. It emits the channels that are then published by the <code>output {}</code> block in <code>main.nf</code>.</p>"},{"location":"pipeline-overview/#main-workflow","title":"Main workflow","text":"<p><code>workflows/sieve.nf</code> defines <code>workflow SIEVE</code>. It always invokes the <code>PREPROCESS</code> subworkflow, then conditionally invokes later subworkflows based on <code>--execute_step</code>:</p> Subworkflow File Purpose <code>PREPROCESS</code> <code>subworkflows/local/preprocess/main.nf</code> Resolve sex map, preprocess raw data or ingest preprocessed data, run grid search or ingest best parameters, and prepare optional validation references. <code>TRAIN_AND_EXPLAIN</code> <code>subworkflows/local/train_and_explain/main.nf</code> Run CV training, select the best checkpoint, explain real data, and run null-baseline comparison when requested. <code>ABLATION</code> <code>subworkflows/local/ablation/main.nf</code> Train and explain L0-L3 ablation models, compare per-level rankings, and generate ablation summaries. <code>EPISTASIS</code> <code>subworkflows/local/epistasis/main.nf</code> Audit co-occurrence, validate non-empty interaction results, perform power analysis, and aggregate gene interactions. <code>VALIDATION</code> <code>subworkflows/local/validation/main.nf</code> Validate discoveries against optional or downloaded references and collect plots."},{"location":"pipeline-overview/#step-resolution","title":"Step resolution","text":"<p><code>lib/sieve_helpers.nf</code> defines <code>resolveExecuteSteps()</code>.</p> <p>If <code>--execute_step</code> is unset, the workflow requests all steps:</p> <pre><code>sex, preprocess, grid, cv, explain, ablation, null, epistasis, validation, plots\n</code></pre> <p>If <code>--execute_step all</code> is supplied, it resolves to the same full set. Individual aliases such as <code>grid_search</code>, <code>cross_validation</code>, and <code>discovery_validation</code> are normalized internally, but the documented stable names are the ten names above.</p>"},{"location":"pipeline-overview/#channel-flow","title":"Channel flow","text":"<p>The practical flow is:</p> <pre><code>raw VCF + phenotypes + sex map\n          |\n          v\nPREPROCESS -&gt; preprocessed.pt -&gt; grid search -&gt; best_params.yaml\n          |                           |\n          v                           v\nTRAIN_AND_EXPLAIN -&gt; CV folds -&gt; best checkpoint -&gt; explain real data\n          |                                      |\n          |                                      +-&gt; validation\n          |                                      +-&gt; epistasis\n          v\nnull baseline -&gt; explain null -&gt; compare attributions -&gt; calibrate/correct rankings\n\nABLATION uses preprocessed data, sex map, best params, and null-preprocessed data\nto create per-level real/null training and attribution comparisons.\n</code></pre>"},{"location":"pipeline-overview/#published-output-model","title":"Published output model","text":"<p>The pipeline uses Nextflow workflow outputs rather than per-process <code>publishDir</code> blocks. The publish destinations are declared in the <code>output {}</code> block of <code>main.nf</code>, with paths such as:</p> <pre><code>${params.cohort_id}/data\n${params.cohort_id}/real_experiments/L3/training\n${params.cohort_id}/attribution_comparison/L3\npipeline_info\n</code></pre> <p>This is why the published tree is grouped by <code>--cohort_id</code>.</p>"},{"location":"reproducibility/","title":"Reproducibility","text":"<p>Reproducibility comes from pinning the pipeline revision, recording parameters, using containers or fixed environments, and preserving execution reports.</p>"},{"location":"reproducibility/#pin-the-pipeline-revision","title":"Pin the pipeline revision","text":"<p>For released versions:</p> <pre><code>nextflow run lescailab/sieve-nextflow \\\n  -r &lt;release&gt; \\\n  -profile docker,gpu \\\n  -params-file params.yml\n</code></pre> <p>For local development, record the Git commit used for the run.</p>"},{"location":"reproducibility/#use-parameter-files","title":"Use parameter files","text":"<p>Keep run parameters in YAML or JSON:</p> <pre><code>vcf: /path/to/data.vcf.gz\nphenotypes: /path/to/phenotypes.tsv\ngenome_build: GRCh38\ncohort_id: cohort_a\noutdir: results\ntrain_device: cuda\n</code></pre> <p>Do not commit real paths or private cohort identifiers. Use local, ignored parameter files for execution-specific values.</p>"},{"location":"reproducibility/#preserve-pipeline-info","title":"Preserve pipeline info","text":"<p>The pipeline writes:</p> <pre><code>pipeline_info/params_&lt;timestamp&gt;.json\npipeline_info/nf_core_sieve_software_versions.yml\npipeline_info/execution_trace_&lt;timestamp&gt;.txt\npipeline_info/execution_report_&lt;timestamp&gt;.html\npipeline_info/execution_timeline_&lt;timestamp&gt;.html\npipeline_info/pipeline_dag_&lt;timestamp&gt;.html\n</code></pre> <p>These files document runtime parameters, software versions, process timing, and task-level execution status.</p>"},{"location":"reproducibility/#containers-and-environments","title":"Containers and environments","text":"<p>Local modules point to a pinned container image:</p> <pre><code>ghcr.io/lescailab/sieve-container:97c6b69bbe1c73b2\n</code></pre> <p>Use a container profile for portable execution:</p> <pre><code>-profile docker,gpu\n</code></pre> <p>or:</p> <pre><code>-profile apptainer,gpu\n</code></pre> <p>Conda and Mamba profiles are also defined, but container execution is easier to reproduce across systems when the image is available.</p>"},{"location":"reproducibility/#randomness","title":"Randomness","text":"<p>Relevant defaults:</p> Parameter Default <code>--train_seed</code> <code>42</code> <code>--null_seed</code> <code>42</code> <code>--null_bootstrap</code> <code>1000</code> <p>Keep these values fixed when comparing runs.</p>"},{"location":"reproducibility/#resume-behavior","title":"Resume behavior","text":"<p>Nextflow <code>-resume</code> can reuse previous task outputs:</p> <pre><code>nextflow run lescailab/sieve-nextflow \\\n  -profile docker,gpu \\\n  -params-file params.yml \\\n  -resume\n</code></pre> <p>Use <code>-resume</code> only when the work directory is preserved and the command still describes the same intended analysis.</p>"},{"location":"reproducibility/#hidden-reproducibility-risks","title":"Hidden reproducibility risks","text":"<ul> <li>Validation references can be downloaded automatically when none are supplied. For fixed analyses, provide versioned local reference files with <code>--clinvar_tsv</code>, <code>--gwas_tsv</code>, and <code>--go_mapping_json</code>.</li> <li><code>--publish_dir_mode move</code> can remove files from work directories. The default is <code>copy</code>.</li> <li>CPU and GPU training may not produce identical floating-point behavior.</li> <li><code>--execute_step</code> can alter which upstream channels are created and therefore which outputs exist.</li> </ul>"},{"location":"running-the-pipeline/","title":"Running the Pipeline","text":"<p>This page collects runnable command patterns. Use placeholder paths and keep real dataset identifiers in local shell commands or parameter files.</p>"},{"location":"running-the-pipeline/#smoke-test","title":"Smoke test","text":"<pre><code>nextflow run . \\\n  -profile test \\\n  -stub-run \\\n  --outdir results_stub\n</code></pre> <p>This command was verified against the current repository. It exercises the complete graph with stub outputs.</p>"},{"location":"running-the-pipeline/#full-raw-input-run","title":"Full raw-input run","text":"<pre><code>nextflow run lescailab/sieve-nextflow \\\n  -profile docker,gpu \\\n  --vcf /path/to/data.vcf.gz \\\n  --phenotypes /path/to/phenotypes.tsv \\\n  --genome_build GRCh38 \\\n  --outdir results\n</code></pre> <p>Use <code>--genome_build GRCh37</code> for GRCh37 data.</p>"},{"location":"running-the-pipeline/#provide-a-sex-map","title":"Provide a sex map","text":"<pre><code>nextflow run lescailab/sieve-nextflow \\\n  -profile docker,gpu \\\n  --vcf /path/to/data.vcf.gz \\\n  --phenotypes /path/to/phenotypes.tsv \\\n  --genome_build GRCh38 \\\n  --infer_sex false \\\n  --sex_map /path/to/sample_sex.tsv \\\n  --outdir results\n</code></pre> <p>Why: this skips <code>sieve-infer-sex</code> and publishes the supplied map as the effective <code>sample_sex.tsv</code>.</p>"},{"location":"running-the-pipeline/#reuse-downstream-artifacts","title":"Reuse downstream artifacts","text":"<pre><code>nextflow run lescailab/sieve-nextflow \\\n  -profile docker,gpu \\\n  --preprocessed_data /path/to/preprocessed.pt \\\n  --sex_map /path/to/sample_sex.tsv \\\n  --best_checkpoint /path/to/best_model.pt \\\n  --checkpoint_config /path/to/config.yaml \\\n  --outdir results\n</code></pre> <p>Why: this avoids raw VCF preprocessing and CV checkpoint selection. The checkpoint config also supplies best-parameter information to branches that need training parameters when <code>--best_params</code> is not supplied.</p>"},{"location":"running-the-pipeline/#limit-execution","title":"Limit execution","text":"<pre><code>nextflow run lescailab/sieve-nextflow \\\n  -profile docker,gpu \\\n  --preprocessed_data /path/to/preprocessed.pt \\\n  --sex_map /path/to/sample_sex.tsv \\\n  --best_checkpoint /path/to/best_model.pt \\\n  --checkpoint_config /path/to/config.yaml \\\n  --execute_step explain,null,validation \\\n  --outdir results\n</code></pre> <p>Use step selection when you know which upstream channels are needed. The workflow exposes shortcut artifacts for sex maps, preprocessed data, best parameters, and best checkpoints; it does not expose a shortcut for every internal channel.</p>"},{"location":"running-the-pipeline/#use-a-parameter-file","title":"Use a parameter file","text":"<p><code>params.yml</code>:</p> <pre><code>vcf: /path/to/data.vcf.gz\nphenotypes: /path/to/phenotypes.tsv\ngenome_build: GRCh38\noutdir: results\ncohort_id: cohort_a\ntrain_device: cuda\n</code></pre> <p>Command:</p> <pre><code>nextflow run lescailab/sieve-nextflow \\\n  -profile docker,gpu \\\n  -params-file params.yml\n</code></pre> <p>Parameter files are the preferred way to make a run repeatable without committing real paths to the repository.</p>"},{"location":"running-the-pipeline/#resume-interrupted-work","title":"Resume interrupted work","text":"<p>Use Nextflow resume when the command and work directory are still valid:</p> <pre><code>nextflow run lescailab/sieve-nextflow \\\n  -profile docker,gpu \\\n  -params-file params.yml \\\n  -resume\n</code></pre> <p><code>-resume</code> reuses cached tasks when inputs, scripts, and relevant parameters match.</p>"},{"location":"troubleshooting/","title":"Troubleshooting","text":"<p>This page lists failure modes verified from code paths and the smoke-test run.</p>"},{"location":"troubleshooting/#missing-vcf","title":"Missing VCF","text":"<p>Error pattern:</p> <pre><code>Missing required argument: --vcf (required by selected execution steps).\n</code></pre> <p>Cause: selected steps need sex inference or preprocessing, and no shortcut artifact satisfies that need.</p> <p>Fix: provide <code>--vcf</code>, or provide the relevant shortcut inputs such as <code>--preprocessed_data</code> and <code>--sex_map</code>.</p>"},{"location":"troubleshooting/#missing-vcf-index","title":"Missing VCF index","text":"<p>Error pattern:</p> <pre><code>Could not find a VCF index for '&lt;vcf&gt;'. Expected '&lt;vcf&gt;.tbi' or '&lt;vcf&gt;.csi'.\n</code></pre> <p>Fix: create an index beside the bgzipped VCF:</p> <pre><code>/path/to/data.vcf.gz.tbi\n</code></pre> <p>or:</p> <pre><code>/path/to/data.vcf.gz.csi\n</code></pre>"},{"location":"troubleshooting/#sex-map-required","title":"Sex map required","text":"<p>Error pattern:</p> <pre><code>The selected execution steps require sex information. Provide --sex_map or set --infer_sex true.\n</code></pre> <p>Cause: sex information is required, no <code>--sex_map</code> was provided, and sex inference is disabled.</p> <p>Fix:</p> <pre><code>--infer_sex true\n</code></pre> <p>or:</p> <pre><code>--infer_sex false --sex_map /path/to/sample_sex.tsv\n</code></pre>"},{"location":"troubleshooting/#checkpoint-pair-mismatch","title":"Checkpoint pair mismatch","text":"<p>Error pattern:</p> <pre><code>Parameters --best_checkpoint and --checkpoint_config must be provided together.\n</code></pre> <p>Fix: supply both files or neither.</p>"},{"location":"troubleshooting/#training-starts-on-cuda-unexpectedly","title":"Training starts on CUDA unexpectedly","text":"<p>Cause: <code>--train_device</code> defaults to <code>cuda</code>.</p> <p>Fix for CPU trials:</p> <pre><code>--train_device cpu\n</code></pre> <p>For GPU runs, combine a container profile with <code>gpu</code>:</p> <pre><code>-profile docker,gpu\n</code></pre>"},{"location":"troubleshooting/#selected-step-produces-few-or-no-outputs","title":"Selected step produces few or no outputs","text":"<p>Cause: <code>--execute_step</code> limits the requested branches, but later branches still need upstream data channels.</p> <p>Fix: either omit <code>--execute_step</code> for a full run, include the required upstream steps, or provide shortcut artifacts where available.</p> <p>Known fragile case: <code>ablation</code> uses the null-preprocessed channel produced by the null branch, but there is no public <code>--null_preprocessed_data</code> shortcut. Prefer a full run or include <code>null</code> when expecting complete ablation comparison outputs.</p>"},{"location":"troubleshooting/#profile-value-warning","title":"Profile value warning","text":"<p>The verified smoke command completed but logged:</p> <pre><code>WARN: nf-core pipelines do not accept positional arguments. The positional argument `test` has been detected.\n</code></pre> <p>This warning appears to come from the local positional-argument parser in <code>main.nf</code>, which tokenizes <code>workflow.commandLine</code> and can treat the <code>-profile</code> value as positional. The run still completed successfully. The parser should be fixed in code; do not document profile values as positional arguments.</p>"},{"location":"troubleshooting/#mkdocs-command-not-found","title":"MkDocs command not found","text":"<p>The local pipeline tooling environment may not include MkDocs. Install Material for MkDocs in a documentation environment:</p> <pre><code>pip install mkdocs-material\n</code></pre> <p>Then run:</p> <pre><code>mkdocs serve -f guidelines/mkdocs.yml\n</code></pre>"},{"location":"troubleshooting/#where-to-inspect-failures","title":"Where to inspect failures","text":"<p>Use:</p> <pre><code>&lt;outdir&gt;/pipeline_info/execution_trace_&lt;timestamp&gt;.txt\n&lt;outdir&gt;/pipeline_info/execution_report_&lt;timestamp&gt;.html\n.nextflow.log\nwork/&lt;task-hash&gt;/.command.err\nwork/&lt;task-hash&gt;/.command.log\n</code></pre> <p>For input and parameter errors, check the early log output from <code>PIPELINE_INITIALISATION</code>.</p>"},{"location":"workflow-steps/","title":"Workflow Steps","text":"<p>This page explains the defined <code>--execute_step</code> names and the subworkflows they activate.</p> <p>Scientific interpretation</p> <p>This page describes pipeline orchestration. Use the framework's Complete Workflow and Command Reference to understand what each SIEVE command computes.</p>"},{"location":"workflow-steps/#step-names","title":"Step names","text":"<p>Stable step names:</p> <pre><code>sex, preprocess, grid, cv, explain, ablation, null, epistasis, validation, plots\n</code></pre> <p>Unset <code>--execute_step</code> means all of them. <code>--execute_step all</code> is equivalent.</p>"},{"location":"workflow-steps/#sex","title":"Sex","text":"<p>Purpose: create the effective <code>sample_sex.tsv</code>.</p> <p>Implementation:</p> <ul> <li>Uses <code>--sex_map</code> directly when provided.</li> <li>Otherwise calls <code>SIEVE_INFER_SEX</code>.</li> <li>Publishes the effective map through <code>SIEVE_EMIT_SEX_MAP</code>.</li> </ul> <p>Key output:</p> <pre><code>&lt;cohort_id&gt;/data/sample_sex.tsv\n</code></pre>"},{"location":"workflow-steps/#preprocess","title":"Preprocess","text":"<p>Purpose: encode raw VCF and phenotype data into a SIEVE tensor dataset.</p> <p>Implementation:</p> <pre><code>sieve-preprocess \\\n  --vcf &lt;vcf&gt; \\\n  --phenotypes &lt;phenotypes&gt; \\\n  --output preprocessed.pt \\\n  --sex-map &lt;sex_map&gt; \\\n  --genome-build &lt;build&gt;\n</code></pre> <p>Shortcut:</p> <pre><code>--preprocessed_data /path/to/preprocessed.pt\n</code></pre> <p>Key output:</p> <pre><code>&lt;cohort_id&gt;/data/preprocessed.pt\n</code></pre>"},{"location":"workflow-steps/#grid","title":"Grid","text":"<p>Purpose: train candidate single-split models and select the best parameter set.</p> <p>Implementation:</p> <ul> <li><code>buildTrainingGrid()</code> expands <code>grid_lr</code>, <code>grid_lambda_attr</code>, <code>grid_latent_dim</code>, <code>grid_hidden_dim</code>, and <code>grid_num_attention_layers</code>.</li> <li><code>SIEVE_TRAIN_SINGLE_GRID</code> runs one training process per grid combination.</li> <li><code>SIEVE_SELECT_BEST_PARAMS</code> emits <code>best_params.yaml</code>.</li> </ul> <p>Shortcut:</p> <pre><code>--best_params /path/to/best_params.yaml\n</code></pre> <p>If <code>--best_checkpoint</code> and <code>--checkpoint_config</code> are supplied without <code>--best_params</code>, the checkpoint config is used as the best-parameter source for downstream branches.</p>"},{"location":"workflow-steps/#cv","title":"CV","text":"<p>Purpose: train cross-validation folds and select the best checkpoint.</p> <p>Implementation:</p> <ul> <li><code>SIEVE_TRAIN_CV</code> creates <code>cv_output/fold_*</code>.</li> <li><code>SIEVE_SELECT_BEST_CHECKPOINT</code> emits <code>best_checkpoint.pt</code>, <code>best_fold_config.yaml</code>, <code>best_fold_id.txt</code>, and <code>cv_folds_summary.tsv</code>.</li> </ul> <p>Shortcut:</p> <pre><code>--best_checkpoint /path/to/best_model.pt --checkpoint_config /path/to/config.yaml\n</code></pre> <p>Key outputs:</p> <pre><code>&lt;cohort_id&gt;/real_experiments/L3/cross_fold/cv_output/fold_*/\n&lt;cohort_id&gt;/real_experiments/L3/training/best_checkpoint.pt\n&lt;cohort_id&gt;/real_experiments/L3/training/best_fold_config.yaml\n</code></pre>"},{"location":"workflow-steps/#explain","title":"Explain","text":"<p>Purpose: run SIEVE explainability on the selected real-data checkpoint.</p> <p>Implementation:</p> <pre><code>sieve-explain \\\n  --experiment-dir _exp_dir \\\n  --preprocessed-data &lt;preprocessed.pt&gt; \\\n  --output-dir explain_output\n</code></pre> <p>Key outputs:</p> <pre><code>sieve_variant_rankings.csv\nsieve_gene_rankings.csv\nsieve_interactions.csv\nexplain_output/attributions.npz\n</code></pre> <p>Published under:</p> <pre><code>&lt;cohort_id&gt;/real_experiments/L3/attributions/\n</code></pre>"},{"location":"workflow-steps/#null","title":"Null","text":"<p>Purpose: create a shuffled/null baseline, train a null model, explain it, and compare real-vs-null attributions.</p> <p>Implementation:</p> <ul> <li><code>SIEVE_CREATE_NULL_BASELINE</code> emits <code>preprocessed_NULL.pt</code>.</li> <li><code>SIEVE_TRAIN_SINGLE_NULL</code> trains the null model.</li> <li><code>SIEVE_EXPLAIN_NULL</code> emits null rankings.</li> <li><code>SIEVE_COMPARE_ATTRIBUTIONS_RAW</code> compares real and null variant rankings.</li> <li><code>SIEVE_BOOTSTRAP_NULL_CALIBRATION</code> calibrates null rankings.</li> <li><code>SIEVE_CORRECT_CHRX_BIAS</code> emits corrected rankings.</li> </ul> <p>Published under:</p> <pre><code>&lt;cohort_id&gt;/null_baselines/L3/\n&lt;cohort_id&gt;/attribution_comparison/L3/\n</code></pre>"},{"location":"workflow-steps/#ablation","title":"Ablation","text":"<p>Purpose: repeat training and attribution comparison for annotation levels <code>L0</code>, <code>L1</code>, <code>L2</code>, and <code>L3</code>.</p> <p>Implementation:</p> <ul> <li>Real ablation training: <code>SIEVE_TRAIN_SINGLE_ABLATION</code></li> <li>Per-level real explainability: <code>SIEVE_EXPLAIN_ABLATION</code></li> <li>Per-level null training and explainability: <code>SIEVE_TRAIN_SINGLE_NULL_ABLATION</code>, <code>SIEVE_EXPLAIN_NULL_ABLATION</code></li> <li>Per-level comparison and correction: <code>SIEVE_COMPARE_ATTRIBUTIONS_ABL</code>, <code>SIEVE_BOOTSTRAP_ABL</code>, <code>SIEVE_CORRECT_CHRX_BIAS_ABL</code></li> <li>Gene lists and cross-level summaries: <code>SIEVE_GENERATE_GENE_LIST</code>, <code>SIEVE_ABLATION_RANKING_COMPARE</code>, <code>SIEVE_PLOT_ABLATION_COMPARISON</code></li> </ul> <p>Published under:</p> <pre><code>&lt;cohort_id&gt;/real_experiments/L*/\n&lt;cohort_id&gt;/null_baselines/L*/\n&lt;cohort_id&gt;/attribution_comparison/L*/\n&lt;cohort_id&gt;/ablation/\n</code></pre> <p>When <code>ablation</code> is selected, <code>SIEVE_CREATE_NULL_BASELINE</code> runs automatically to produce the shuffled null dataset, regardless of whether <code>null</code> is also selected. Running <code>--execute_step ablation</code> therefore always produces per-level null comparisons. Including <code>null</code> additionally runs the full L3 null training, explanation, and attribution comparison.</p>"},{"location":"workflow-steps/#epistasis","title":"Epistasis","text":"<p>Purpose: audit co-occurrence, test candidate interactions when present, quantify power, and aggregate gene interactions.</p> <p>Implementation:</p> <ul> <li><code>SIEVE_AUDIT_COOCCURRENCE</code> always runs when <code>epistasis</code> is selected.</li> <li><code>SIEVE_VALIDATE_EPISTASIS</code> runs only when <code>sieve_interactions.csv</code> contains non-empty interaction rows.</li> <li><code>SIEVE_EPISTASIS_POWER_ANALYSIS</code> uses co-occurrence outputs plus optional null attribution data.</li> <li><code>SIEVE_AGGREGATE_GENE_INTERACTIONS</code> aggregates gene-level interaction tables and network files.</li> </ul> <p>Published under:</p> <pre><code>&lt;cohort_id&gt;/epistasis/L3/\n</code></pre>"},{"location":"workflow-steps/#validation","title":"Validation","text":"<p>Purpose: compare discovery outputs against optional or downloaded reference resources.</p> <p>Implementation:</p> <ul> <li><code>SIEVE_DOWNLOAD_REFERENCES</code> runs only when <code>validation</code> is selected and no validation resources are supplied.</li> <li><code>SIEVE_VALIDATE_DISCOVERIES</code> validates variant and gene rankings.</li> </ul> <p>Published under:</p> <pre><code>&lt;cohort_id&gt;/validation/\n</code></pre>"},{"location":"workflow-steps/#plots","title":"Plots","text":"<p>Purpose: collect plots from upstream process output directories.</p> <p>Implementation:</p> <ul> <li><code>SIEVE_COLLECT_PLOTS</code> receives accumulated plot-source directories.</li> <li>Running only <code>--execute_step plots</code> can produce an empty or minimal plot collection if no selected upstream steps generated plot sources.</li> </ul>"}]}